<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Travel Go</title>
    <style>
        body { font-family: Arial, sans-serif; text-align: center; padding: 50px; }
        .btn { 
            padding: 10px 20px; 
            margin: 20px; 
            font-size: 18px; 
            border: none; 
            cursor: pointer; 
            background-color: #007bff; 
            color: white; 
            border-radius: 5px;
        }
        .btn:hover { background-color: #0056b3; }
    </style>
</head>
<body>

    <h1>Welcome to Travel Go</h1>
    <p>Plan your perfect trip across India!</p>

    <% if (!userId) { %>
        <a class="btn" href="/login">Login</a>
        <a class="btn" href="/signup">Sign Up</a>
    <% } else { %>
        <h2>Welcome to Your Travel Planner</h2>
        <button class="btn" onclick="location.href='/startplanning'">Start Planning</button>
        <button class="btn" onclick="location.href='/saved-plans'">Saved Plans</button>
    <% } %>

</body>
</html>
